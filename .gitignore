node_modules
dist
coverage
*.log
.DS_Store
test/fixtures/prisma/generated

# Local-only project/instructions docs — never tracked. Kept on disk for local
# reference, deliberately excluded from the public repo and its history.
CLAUDE.md
hookkit-testing.md

# Environment / secrets — never commit these
.env
.env.*
!.env.example

# testcontainers / Docker local artifacts
.testcontainers.properties
docker-compose.override.yml

# IDE / OS
.vscode
.idea
Thumbs.db
